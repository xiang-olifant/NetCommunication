import XCTest
@testable import NetCommunication

final class NetCommunicationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetCommunication().text, "Hello, World!")
    }
}
